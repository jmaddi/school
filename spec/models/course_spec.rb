require 'rails_helper'

RSpec.describe Course, :type => :model do
  it 'should not allow course without name' do
  end
  it 'should not allow course without code'
  it 'should not allow course without start date'
  it 'should not allow course without max enrollments'
  it 'should not allow multiple coures with same code'
end
